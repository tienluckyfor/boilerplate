{
  "name": "minify-css-js",
  "version": "1.0.0",
  "description": "``` npm install -g grunt-cli npm init npm install -S grunt ``` ## Gruntfile.js ``` touch Gruntfile.js module.exports = function(grunt){     grunt.registerTask('speak', function) }; ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "grunt": "^1.1.0"
  },
  "devDependencies": {
    "grunt-contrib-concat": "^1.0.1",
    "grunt-contrib-cssmin": "^3.0.0",
    "grunt-contrib-uglify": "^4.0.1",
    "grunt-contrib-watch": "^1.1.0"
  }
}
